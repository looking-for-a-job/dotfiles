отдельный репозиторий с tests badges?
да но а если их очень много и они не в каждом репозитории. хотя можно пройтись проверить test -d tests/


readme из bash шаблонов?

[[ -z "$README_USAGE" ]] && {
###
${README_USAGE}
}